program MJProgram
    int x;
    int y;
    int a[], b, c[];
    const int ac = 'a', bc = 2483, cc = false;
    const int d = 1242;
{
    bool method1() int x; {
        x = - 'a' + 1 % x + false / true * false - x * 121;
        x = 3 * m2() * (3 + 8 + new int[4 - 3 ]);
        x = true + x[2];

        { print(1212, 1); }

        if (x < 3 || x > 6) {
            x++;
            if (x < 2) x++;
            if (x < 0) x++; else x--;
        }
        else {
            x--;
            x--;
        }

        x = switch (x + 2) {
          case 0: yield x + 2;
          case 3: {
            yield -x + 3;
          }
          default: {
            print(x, 2);
            yield 3;
          }
        };

        x = switch (1) {
          default: yield 3;
        };

        return 1;
    }

    int m2(int b, int c[], int d, int x[])
        int x2, x3;
        int x4[], x5, x7[];
    {
        print(1212);

        do
        {
            x4[2] ++;
            x3--;
            method1(12, 'a');
            method1();

            if (x2 > 2)
                continue;
            else {
                x2++;
                break;
            }

            method1(true, 'b', '4', false, 12131, 'c');

            do return true; while (false);
        }
        while (a >= b && c > 5 || d != d || (a + b) == 2 && x2[1] < 3);

        read(x4[1]);

        { }

        return 'a';
    }

    void main(int c)
    {
    }
}
